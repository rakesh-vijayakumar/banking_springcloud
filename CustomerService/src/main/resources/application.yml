spring:
  application:
    name: banking-customer-api
  data:
    mongodb:
      host: localhost
      port: 27017
      database: banking

server:
  port: 9015

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:9000/eureka/
